// ── Publish row ────────────────────────────────────────────────────
// Active state is controlled by `isBusy` (posting/uploading) and the
// upload progress text is driven by `uploading` + `uploadProgress`, not
// the older `uploadState` shape that was removed from the composer.
